import React from 'react';

const ProtectedRoute = ({ children }) => {
  // Authentication check removed to allow free navigation for demonstration
  return children;
};

export default ProtectedRoute;
